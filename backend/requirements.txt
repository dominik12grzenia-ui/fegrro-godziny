# FeGrro backend - minimal production dependencies.
# Pinned versions MUST have prebuilt wheels for Python 3.11 AND 3.14
# (Render currently defaults to 3.14 if runtime.txt is missing).
# Verified: pydantic-core 2.41.1 has cp314 wheels (Render: python3.14).
# DO NOT pip freeze - keep this file curated.

# FastAPI stack
fastapi==0.118.0
uvicorn==0.32.1
starlette==0.47.2
pydantic==2.12.0
pydantic_core==2.41.1
email-validator==2.2.0
python-multipart==0.0.20
anyio==4.7.0
sniffio==1.3.1
h11==0.14.0
typing-inspection==0.4.2
annotated-types==0.7.0

# MongoDB
motor==3.6.0
pymongo==4.9.2
dnspython==2.7.0

# HTTP
httpx==0.28.1
httpcore==1.0.7
requests==2.32.3
charset-normalizer==3.4.0
idna==3.10
urllib3==2.2.3
certifi==2024.12.14

# Auth (JWT + bcrypt)
python-jose==3.3.0
ecdsa==0.19.0
rsa==4.9
passlib==1.7.4
bcrypt==4.2.1
cryptography==44.0.0
cffi==1.17.1
pycparser==2.22

# Scheduler
APScheduler==3.11.0
tzlocal==5.2
tzdata==2024.2
pytz==2024.2

# Excel / Office (MS Graph OneDrive)
openpyxl==3.1.5
et_xmlfile==2.0.0
msal==1.31.1
PyJWT==2.10.1

# PDF
reportlab==4.2.5
pillow==11.0.0

# Web Push (VAPID)
pywebpush==2.0.1
py-vapid==1.9.1
http_ece==1.2.0

# Misc
python-dotenv==1.0.1
python-dateutil==2.9.0
six==1.17.0
typing_extensions==4.14.1
click==8.1.7
pyotp==2.9.0
qrcode==8.2

# AI - Emergent LLM integration (Claude/OpenAI/Gemini via EMERGENT_LLM_KEY).
# Custom Emergent index — supports pip native --extra-index-url in requirements.
# Bez tej linii produkcyjny build (Render) wywala "No module named 'emergentintegrations'".
--extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/
emergentintegrations==0.1.0
